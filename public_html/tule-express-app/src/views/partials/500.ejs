<%- include('head', { pageTitle }) %>
<%- include('navbar') %>
<%- include('flash') %>

<section class="container py-5">
  <div class="card p-4 shadow-sm border-danger">
    <h1 class="h4 mb-2">Ocurrió un error</h1>
    <p class="text-muted">No se preocupen: el sistema registró el evento. Intenta nuevamente.</p>
    <div class="small text-muted">ID: <%= errorId %></div>
    <div class="mt-3">
      <a class="btn btn-outline-dark" href="/">Volver al inicio</a>
    </div>
  </div>
</section>

<%- include('footer') %>
